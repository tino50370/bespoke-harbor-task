[project]
name = "bespoke-harbor-task"
version = "0.1.0"
description = "A benchmarking framework for testing AI agents on DevOps problems using the Harbor tool"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
